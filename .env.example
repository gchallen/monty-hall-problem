# MongoDB Configuration (Optional)
# If provided, game results will be persisted to MongoDB
# If not provided, data will be stored in memory only
MONGODB_URI=mongodb://localhost:27017/monty-hall

# Example for MongoDB Atlas:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/monty-hall?retryWrites=true&w=majority